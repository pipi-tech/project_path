======================================================
  MASTER SRC REGISTRY
  generated: 2026-04-29 23:22:15
  node: pop-os  user: maven
======================================================

[ project_path structure ]
/home/maven/project/project_path
├── builder
│   ├── input
│   │   └── test.toml
│   ├── output
│   └── src
│       ├── build.sh
│       ├── compare_deps.sh
│       ├── drift.sh
│       ├── gen_fingerprint.sh
│       ├── install_deps.sh
│       ├── read_toml.sh
│       ├── register.sh
│       └── stamp_folders.sh
├── cmd
│   ├── builder
│   │   ├── build.sh
│   │   ├── compare_deps.sh
│   │   ├── drift.sh
│   │   └── fingerprint.sh
│   ├── cli
│   │   ├── master_log.sh
│   │   ├── master_src.sh
│   │   └── watch_silent.sh
│   ├── combine.sh
│   ├── config
│   │   ├── add_dep.sh
│   │   ├── classify_deps.sh
│   │   ├── default
│   │   ├── env
│   │   ├── iso
│   │   ├── local
│   │   ├── migrate_toml.sh
│   │   ├── onboard_deps.sh
│   │   ├── pkg_classes.conf
│   │   ├── remote
│   │   ├── set_log_domains.sh
│   │   ├── template
│   │   │   └── blank.toml
│   │   ├── type_registry.conf
│   │   └── venv
│   ├── dep
│   │   ├── diff_deps.sh
│   │   ├── snap_after.sh
│   │   └── snap_before.sh
│   ├── main
│   ├── snap
│   ├── terminal
│   └── tui
│       ├── dashboard.py
│       ├── modals
│       │   ├── __init__.py
│       │   └── new_project.py
│       ├── panes
│       │   ├── __init__.py
│       │   ├── keybinds.py
│       │   ├── logs.py
│       │   ├── nav.py
│       │   ├── projects.py
│       │   ├── snaps.py
│       │   └── toml_view.py
│       └── styles
│           └── fsf.py
├── cmd_list 
├── data
│   ├── log
│   │   ├── builder
│   │   │   ├── input
│   │   │   │   ├── build_done
│   │   │   │   ├── build_start
│   │   │   │   ├── build_step
│   │   │   │   ├── dep_install
│   │   │   │   └── drift
│   │   │   ├── output
│   │   │   └── src
│   │   │       ├── on_build_done.sh
│   │   │       ├── on_build_start.sh
│   │   │       ├── on_build_step.sh
│   │   │       ├── on_dep_install.sh
│   │   │       └── on_drift.sh
│   │   ├── cli
│   │   │   ├── input
│   │   │   ├── output
│   │   │   └── src
│   │   ├── config
│   │   │   ├── input
│   │   │   ├── output
│   │   │   ├── snap
│   │   │   │   ├── cold
│   │   │   │   ├── hot
│   │   │   │   └── warm
│   │   │   └── src
│   │   ├── dep
│   │   │   ├── input
│   │   │   │   ├── after
│   │   │   │   ├── before
│   │   │   │   ├── diff
│   │   │   │   ├── install
│   │   │   │   ├── managers
│   │   │   │   └── silent
│   │   │   ├── output
│   │   │   └── src
│   │   │       ├── detect_manager.sh.bak
│   │   │       ├── diff_deps.sh.bak
│   │   │       ├── on_install.sh
│   │   │       ├── snap_after.sh.bak
│   │   │       └── snap_before.sh.bak
│   │   ├── logScr.sh
│   │   ├── main
│   │   │   ├── input
│   │   │   ├── output
│   │   │   └── src
│   │   ├── MasterLOGText
│   │   │   └── master_log.txt
│   │   ├── MasterSCRText
│   │   │   └── master_src.txt
│   │   ├── MasterTree
│   │   │   └── tree.txt
│   │   ├── registry
│   │   │   ├── input
│   │   │   │   ├── accessed
│   │   │   │   ├── drift
│   │   │   │   ├── registered
│   │   │   │   └── updated
│   │   │   ├── output
│   │   │   └── src
│   │   │       ├── on_access.sh
│   │   │       ├── on_drift.sh
│   │   │       ├── on_register.sh
│   │   │       └── on_update.sh
│   │   ├── template
│   │   ├── terminal
│   │   │   ├── input
│   │   │   │   ├── project
│   │   │   │   ├── terminal
│   │   │   │   ├── when
│   │   │   │   └── who
│   │   │   ├── output
│   │   │   │   └── session.log
│   │   │   └── src
│   │   │       ├── project.sh
│   │   │       ├── terminal.sh
│   │   │       ├── when.sh
│   │   │       └── who.sh
│   │   └── tui
│   │       ├── input
│   │       ├── output
│   │       └── src
│   ├── registry.conf
│   ├── registry.txt
│   └── template
│       ├── scaffold.sh
│       └── structure.txt
├── engines
│   ├── builder
│   │   ├── input
│   │   │   └── test.toml
│   │   ├── output
│   │   └── src
│   │       ├── build.sh
│   │       ├── compare_deps.sh
│   │       ├── drift.sh
│   │       ├── gen_fingerprint.sh
│   │       ├── install_deps.sh
│   │       ├── read_toml.sh
│   │       ├── register.sh
│   │       └── stamp_folders.sh
│   ├── drift_engine
│   │   ├── input
│   │   ├── output
│   │   └── src
│   │       └── main.sh
│   ├── log_engine
│   │   ├── input
│   │   ├── output
│   │   └── src
│   │       └── main.sh
│   ├── network
│   │   ├── input
│   │   ├── output
│   │   └── src
│   │       └── main.sh
│   └── state_engine
│       ├── input
│       ├── output
│       └── src
│           └── main.sh
├── functions
│   ├── builder.sh
│   ├── cli.sh
│   ├── config.sh
│   ├── dep_wrappers.sh
│   ├── logs.sh
│   ├── new.sh
│   ├── proj.sh
│   ├── registry.sh
│   ├── template.sh
│   └── tui.sh
├── functions.sh
├── overlay
│   ├── base
│   ├── merged
│   ├── mid
│   └── top
├── overlay_engine.sh
├── project.toml
├── scaffold.sh
├── tests
│   ├── build_sim
│   ├── concurrency
│   ├── drift_sim
│   └── state_sim
└── v1Goal.txt

[ project_path src ]

===================( builder/src/build.sh )====================
#!/bin/bash
TOML="$1"
[ -z "$TOML" ] && echo "usage: build.sh <path/to/project.toml>" && exit 1

${EDITOR:-nano} "$TOML"

SRC=~/project/project_path/builder/src
CMD_CFG=~/project/project_path/cmd/config
LOG=~/project/project_path/data/log/builder/src
TS=$(date '+%Y-%m-%d_%H-%M-%S')

echo "--- reading toml"
source "$SRC/read_toml.sh" "$TOML"

bash "$LOG/on_build_start.sh" "$PROJECT_NAME" "$TS" "$TOML"

echo "--- stamping folders"
bash "$SRC/stamp_folders.sh"
bash "$LOG/on_build_step.sh" "$PROJECT_NAME" "$TS" "stamp_folders" "done"

echo "--- setting log domains"
bash "$CMD_CFG/set_log_domains.sh" "$TOML"
bash "$LOG/on_build_step.sh" "$PROJECT_NAME" "$TS" "set_log_domains" "done"

echo "--- installing deps"
bash "$SRC/install_deps.sh" "$TOML"
bash "$LOG/on_build_step.sh" "$PROJECT_NAME" "$TS" "install_deps" "done"

echo "--- generating fingerprint"
source "$SRC/gen_fingerprint.sh" "$TOML"
bash "$LOG/on_build_step.sh" "$PROJECT_NAME" "$TS" "gen_fingerprint" "done"

echo "--- comparing deps"
bash "$SRC/compare_deps.sh" "$TOML"
bash "$LOG/on_build_step.sh" "$PROJECT_NAME" "$TS" "compare_deps" "done"

echo "--- checking drift"
bash "$SRC/drift.sh"
bash "$LOG/on_build_step.sh" "$PROJECT_NAME" "$TS" "drift_check" "done"

echo "--- registering"
bash "$SRC/register.sh" "$TOML"
bash "$LOG/on_build_step.sh" "$PROJECT_NAME" "$TS" "register" "done"

echo "--- moving toml to project root"
PROJECT_ROOT=~/project/$PROJECT_NAME
cp "$TOML" "$PROJECT_ROOT/project.toml"
echo "toml saved: $PROJECT_ROOT/project.toml"

bash "$LOG/on_build_done.sh" "$PROJECT_NAME" "$TS" "$FINGERPRINT_HASH" ""

echo "--- resetting template"
cp "$CMD_CFG/template/blank.toml" \
   ~/project/project_path/builder/input/test.toml
echo "template reset"

echo "--- done: $PROJECT_NAME"
===================( end )====================

===================( builder/src/compare_deps.sh )====================
#!/bin/bash
TOML="$1"
[ -z "$TOML" ] && echo "no toml provided" && exit 1

EXPECTED=$(grep "^requires" "$TOML" | cut -d'=' -f2 | tr -d '[]" ' | tr ',' '\n' | sort)
INSTALLED=$(pip freeze 2>/dev/null | cut -d'=' -f1 | tr '[:upper:]' '[:lower:]' | sort)

echo "--- dep comparison"
while IFS= read -r dep; do
  [ -z "$dep" ] && continue
  if echo "$INSTALLED" | grep -qi "^$dep$"; then
    echo "  ✓ $dep"
  else
    echo "  ✗ $dep MISSING"
  fi
done <<< "$EXPECTED"
===================( end )====================

===================( builder/src/drift.sh )====================
#!/bin/bash
[ -z "$PROJECT_NAME" ] && echo "no project name" && exit 1
REG_CONF=~/project/project_path/data/registry.conf
TS=$(date '+%Y-%m-%d_%H-%M-%S')
STORED_HASH=$(grep -i "^$PROJECT_NAME:" "$REG_CONF" 2>/dev/null | cut -d: -f6)
CURRENT_HASH=$(pip freeze 2>/dev/null | sha256sum | cut -d' ' -f1)
if [ -z "$STORED_HASH" ]; then
  echo "no previous fingerprint — skipping drift check"
  exit 0
fi
if [ "$STORED_HASH" != "$CURRENT_HASH" ]; then
  echo "  ⚠ drift detected"
  echo "  stored:  $STORED_HASH"
  echo "  current: $CURRENT_HASH"
  bash ~/project/project_path/data/log/registry/src/on_drift.sh \
    "$PROJECT_NAME" "$STORED_HASH" "$CURRENT_HASH" "$TS"
else
  echo "  ✓ no drift"
fi
===================( end )====================

===================( builder/src/gen_fingerprint.sh )====================
#!/bin/bash
[ -z "$PROJECT_NAME" ] && echo "no project name" && exit 1
TOML="$1"
TS=$(date '+%Y-%m-%d_%H-%M-%S')
HASH=$(pip freeze 2>/dev/null | sha256sum | cut -d' ' -f1)

echo "hash=$HASH"
echo "generated_at=$TS"
echo "generated_by=$USER"

export FINGERPRINT_HASH=$HASH
export FINGERPRINT_TS=$TS

[ -z "$TOML" ] && exit 0
[ ! -f "$TOML" ] && exit 0

sed -i "s|^hash.*=.*|hash            = \"$HASH\"|" "$TOML"
sed -i "s|^generated_at.*=.*|generated_at    = \"$TS\"|" "$TOML"
sed -i "s|^generated_by.*=.*|generated_by    = \"$USER\"|" "$TOML"
===================( end )====================

===================( builder/src/install_deps.sh )====================
#!/bin/bash
TOML="$1"
[ -z "$TOML" ] && echo "no toml provided" && exit 1

DEPS=$(grep "^requires" "$TOML" | cut -d'=' -f2 | tr -d '[]"' | tr ',' ' ')

[ -z "$DEPS" ] && echo "no dependencies found" && exit 0

echo "installing: $DEPS"
pip install $DEPS --break-system-packages
===================( end )====================

===================( builder/src/read_toml.sh )====================
#!/bin/bash
TOML="$1"
[ -z "$TOML" ] && echo "no toml provided" && exit 1
[ ! -f "$TOML" ] && echo "toml not found: $TOML" && exit 1

get_val() {
  grep "^$1" "$TOML" | cut -d'=' -f2 | tr -d ' "' 
}

PROJECT_NAME=$(get_val "name")
PROJECT_TYPE=$(get_val "domain")
PROJECT_ENV=$(get_val "env")
PROJECT_PYTHON=$(get_val "python")

export PROJECT_NAME PROJECT_TYPE PROJECT_ENV PROJECT_PYTHON
===================( end )====================

===================( builder/src/register.sh )====================
#!/bin/bash
[ -z "$PROJECT_NAME" ] && echo "no project name" && exit 1

REG_LOG=~/project/project_path/data/registry.txt
REG_CONF=~/project/project_path/data/registry.conf
TOML="$1"
TS=$(date '+%Y-%m-%d_%H-%M-%S')
NODE=$(hostname)
UUID=$(cat /proc/sys/kernel/random/uuid)

echo "$TS :: $USER :: $PROJECT_NAME :: $PROJECT_TYPE :: $PROJECT_ENV :: $FINGERPRINT_HASH" >> "$REG_LOG"

if ! grep -q "^$PROJECT_NAME:" "$REG_CONF" 2>/dev/null; then
  echo "$PROJECT_NAME:~/project/$PROJECT_NAME:$PROJECT_VENV:$PROJECT_ENV:$PROJECT_TYPE" >> "$REG_CONF"
  bash ~/project/project_path/data/log/registry/src/on_register.sh "$PROJECT_NAME" "$TS"
else
  sed -i "s|^$PROJECT_NAME:.*|$PROJECT_NAME:~/project/$PROJECT_NAME:$PROJECT_VENV:$PROJECT_ENV:$PROJECT_TYPE|" "$REG_CONF"
  bash ~/project/project_path/data/log/registry/src/on_update.sh "$PROJECT_NAME" "$TS"
fi

[ -f "$TOML" ] && sed -i "s|^project_id.*=.*|project_id  = \"$UUID\"|" "$TOML"
[ -f "$TOML" ] && sed -i "s|^node.*=.*|node        = \"$NODE\"|" "$TOML"

echo "registered: $PROJECT_NAME :: $UUID :: $NODE"
===================( end )====================

===================( builder/src/stamp_folders.sh )====================
#!/bin/bash
[ -z "$PROJECT_NAME" ] && echo "no project name" && exit 1
BASE="$HOME/project/$PROJECT_NAME"
mkdir -p "$BASE"/{src,input,output,tests,docs}
echo "stamped: $BASE"
===================( end )====================

===================( data/log/builder/src/on_build_done.sh )====================
#!/bin/bash
[ -z "$1" ] && exit 0
DIR=~/project/project_path/data/log/builder/input/build_done
mkdir -p "$DIR"
echo "project=$1" > "$DIR/$2.txt"
echo "fingerprint=$3" >> "$DIR/$2.txt"
echo "uuid=$4" >> "$DIR/$2.txt"
echo "user=$USER" >> "$DIR/$2.txt"
===================( end )====================

===================( data/log/builder/src/on_build_start.sh )====================
#!/bin/bash
[ -z "$1" ] && exit 0
DIR=~/project/project_path/data/log/builder/input/build_start
mkdir -p "$DIR"
echo "project=$1" > "$DIR/$2.txt"
echo "toml=$3" >> "$DIR/$2.txt"
echo "user=$USER" >> "$DIR/$2.txt"
echo "node=$(hostname)" >> "$DIR/$2.txt"
===================( end )====================

===================( data/log/builder/src/on_build_step.sh )====================
#!/bin/bash
[ -z "$1" ] && exit 0
DIR=~/project/project_path/data/log/builder/input/build_step
mkdir -p "$DIR"
echo "project=$1" > "$DIR/$2.txt"
echo "step=$3" >> "$DIR/$2.txt"
echo "status=$4" >> "$DIR/$2.txt"
===================( end )====================

===================( data/log/builder/src/on_dep_install.sh )====================
#!/bin/bash
[ -z "$1" ] && exit 0
DIR=~/project/project_path/data/log/builder/input/dep_install
mkdir -p "$DIR"
echo "project=$1" > "$DIR/$2.txt"
echo "dep=$3" >> "$DIR/$2.txt"
echo "status=$4" >> "$DIR/$2.txt"
===================( end )====================

===================( data/log/builder/src/on_drift.sh )====================
#!/bin/bash
[ -z "$1" ] && exit 0
DIR=~/project/project_path/data/log/builder/input/drift
mkdir -p "$DIR"
echo "project=$1" > "$DIR/$2.txt"
echo "stored=$3" >> "$DIR/$2.txt"
echo "current=$4" >> "$DIR/$2.txt"
===================( end )====================

===================( data/log/dep/src/on_install.sh )====================
#!/bin/bash
[ -z "$1" ] && exit 0
PROJECT="$1"
TS="$2"
MANAGER="$3"
PACKAGE="$4"
PARENT_PID="${5:-$$}"
DIR=~/project/project_path/data/log/dep/input/install
mkdir -p "$DIR"

SESSION_HASH=$(cat "/tmp/pp_session_${PARENT_PID}.hash" 2>/dev/null || echo "no-session")

echo "project=$PROJECT"          > "$DIR/$TS.txt"
echo "ts=$TS"                   >> "$DIR/$TS.txt"
echo "user=$USER"               >> "$DIR/$TS.txt"
echo "session_hash=$SESSION_HASH" >> "$DIR/$TS.txt"
echo "manager=$MANAGER"         >> "$DIR/$TS.txt"
echo "package=$PACKAGE"         >> "$DIR/$TS.txt"
echo "node=$(hostname)"         >> "$DIR/$TS.txt"
===================( end )====================

===================( data/log/registry/src/on_access.sh )====================
#!/bin/bash
[ -z "$1" ] && exit 0
DIR=~/project/project_path/data/log/registry/input/accessed
mkdir -p "$DIR"
echo "$USER :: $1 :: $2" > "$DIR/$2.txt"
===================( end )====================

===================( data/log/registry/src/on_drift.sh )====================
#!/bin/bash
[ -z "$1" ] && exit 0
DIR=~/project/project_path/data/log/registry/input/drift
mkdir -p "$DIR"
echo "$USER :: $1 :: expected=$2 :: got=$3 :: $4" > "$DIR/$4.txt"
===================( end )====================

===================( data/log/registry/src/on_register.sh )====================
#!/bin/bash
[ -z "$1" ] && exit 0
DIR=~/project/project_path/data/log/registry/input/registered
mkdir -p "$DIR"
echo "$USER :: $1 :: $2" > "$DIR/$2.txt"
===================( end )====================

===================( data/log/registry/src/on_update.sh )====================
#!/bin/bash
[ -z "$1" ] && exit 0
DIR=~/project/project_path/data/log/registry/input/updated
mkdir -p "$DIR"
echo "$USER :: $1 :: $2" > "$DIR/$2.txt"
===================( end )====================

===================( data/log/terminal/src/project.sh )====================
#!/bin/bash
DIR=~/project/project_path/data/log/terminal/input/project
mkdir -p "$DIR"
echo "$1" > "$DIR/$2.txt"
===================( end )====================

===================( data/log/terminal/src/terminal.sh )====================
#!/bin/bash
DIR=~/project/project_path/data/log/terminal/input/terminal
mkdir -p "$DIR"
TTY_VAL=$(tty 2>/dev/null || echo "no-tty")
echo "TERM: $TERM" > "$DIR/$2.txt"
echo "TTY: $TTY_VAL" >> "$DIR/$2.txt"
echo "PPID: $PPID" >> "$DIR/$2.txt"
echo "SESSION: $XDG_SESSION_ID" >> "$DIR/$2.txt"
echo "DISPLAY: $DISPLAY" >> "$DIR/$2.txt"
===================( end )====================

===================( data/log/terminal/src/when.sh )====================
#!/bin/bash
TS=$(date '+%Y-%m-%d_%H-%M-%S')
DIR=~/project/project_path/data/log/terminal/input/when
mkdir -p "$DIR"
echo "$TS" > "$DIR/$TS.txt"
===================( end )====================

===================( data/log/terminal/src/who.sh )====================
#!/bin/bash
[ -z "$2" ] && exit 0
DIR=~/project/project_path/data/log/terminal/input/who
mkdir -p "$DIR"

TS="$2"
PARENT_PID="${3:-$$}"
SESSION_ID="${XDG_SESSION_ID:-$$}"
TTY_VAL=$(tty 2>/dev/null || echo "no-tty")
DISPLAY_VAL="${DISPLAY:-no-display}"

USER_HASH=$(echo "$USER|$(hostname)|$TTY_VAL|$SESSION_ID|$TS|$DISPLAY_VAL" \
  | sha256sum | cut -d' ' -f1)

echo "$USER"                  > "$DIR/$TS.txt"
echo "hash=$USER_HASH"       >> "$DIR/$TS.txt"
echo "session=$SESSION_ID"   >> "$DIR/$TS.txt"
echo "tty=$TTY_VAL"          >> "$DIR/$TS.txt"
echo "display=$DISPLAY_VAL"  >> "$DIR/$TS.txt"
echo "ppid=$PARENT_PID"      >> "$DIR/$TS.txt"

echo "$USER_HASH" > "/tmp/pp_session_${PARENT_PID}.hash"
===================( end )====================

===================( engines/builder/src/build.sh )====================
#!/bin/bash
TOML="$1"
[ -z "$TOML" ] && echo "usage: build.sh <path/to/project.toml>" && exit 1

${EDITOR:-nano} "$TOML"

SRC=~/project/project_path/builder/src
CMD_CFG=~/project/project_path/cmd/config
LOG=~/project/project_path/data/log/builder/src
TS=$(date '+%Y-%m-%d_%H-%M-%S')

echo "--- reading toml"
source "$SRC/read_toml.sh" "$TOML"

bash "$LOG/on_build_start.sh" "$PROJECT_NAME" "$TS" "$TOML"

echo "--- stamping folders"
bash "$SRC/stamp_folders.sh"
bash "$LOG/on_build_step.sh" "$PROJECT_NAME" "$TS" "stamp_folders" "done"

echo "--- setting log domains"
bash "$CMD_CFG/set_log_domains.sh" "$TOML"
bash "$LOG/on_build_step.sh" "$PROJECT_NAME" "$TS" "set_log_domains" "done"

echo "--- installing deps"
bash "$SRC/install_deps.sh" "$TOML"
bash "$LOG/on_build_step.sh" "$PROJECT_NAME" "$TS" "install_deps" "done"

echo "--- generating fingerprint"
source "$SRC/gen_fingerprint.sh" "$TOML"
bash "$LOG/on_build_step.sh" "$PROJECT_NAME" "$TS" "gen_fingerprint" "done"

echo "--- comparing deps"
bash "$SRC/compare_deps.sh" "$TOML"
bash "$LOG/on_build_step.sh" "$PROJECT_NAME" "$TS" "compare_deps" "done"

echo "--- checking drift"
bash "$SRC/drift.sh"
bash "$LOG/on_build_step.sh" "$PROJECT_NAME" "$TS" "drift_check" "done"

echo "--- registering"
bash "$SRC/register.sh" "$TOML"
bash "$LOG/on_build_step.sh" "$PROJECT_NAME" "$TS" "register" "done"

echo "--- moving toml to project root"
PROJECT_ROOT=~/project/$PROJECT_NAME
cp "$TOML" "$PROJECT_ROOT/project.toml"
echo "toml saved: $PROJECT_ROOT/project.toml"

bash "$LOG/on_build_done.sh" "$PROJECT_NAME" "$TS" "$FINGERPRINT_HASH" ""

echo "--- resetting template"
cp "$CMD_CFG/template/blank.toml" \
   ~/project/project_path/builder/input/test.toml
echo "template reset"

echo "--- done: $PROJECT_NAME"
===================( end )====================

===================( engines/builder/src/compare_deps.sh )====================
#!/bin/bash
TOML="$1"
[ -z "$TOML" ] && echo "no toml provided" && exit 1

EXPECTED=$(grep "^requires" "$TOML" | cut -d'=' -f2 | tr -d '[]" ' | tr ',' '\n' | sort)
INSTALLED=$(pip freeze 2>/dev/null | cut -d'=' -f1 | tr '[:upper:]' '[:lower:]' | sort)

echo "--- dep comparison"
while IFS= read -r dep; do
  [ -z "$dep" ] && continue
  if echo "$INSTALLED" | grep -qi "^$dep$"; then
    echo "  ✓ $dep"
  else
    echo "  ✗ $dep MISSING"
  fi
done <<< "$EXPECTED"
===================( end )====================

===================( engines/builder/src/drift.sh )====================
#!/bin/bash
[ -z "$PROJECT_NAME" ] && echo "no project name" && exit 1
REG_CONF=~/project/project_path/data/registry.conf
TS=$(date '+%Y-%m-%d_%H-%M-%S')
STORED_HASH=$(grep -i "^$PROJECT_NAME:" "$REG_CONF" 2>/dev/null | cut -d: -f6)
CURRENT_HASH=$(pip freeze 2>/dev/null | sha256sum | cut -d' ' -f1)
if [ -z "$STORED_HASH" ]; then
  echo "no previous fingerprint — skipping drift check"
  exit 0
fi
if [ "$STORED_HASH" != "$CURRENT_HASH" ]; then
  echo "  ⚠ drift detected"
  echo "  stored:  $STORED_HASH"
  echo "  current: $CURRENT_HASH"
  bash ~/project/project_path/data/log/registry/src/on_drift.sh \
    "$PROJECT_NAME" "$STORED_HASH" "$CURRENT_HASH" "$TS"
else
  echo "  ✓ no drift"
fi
===================( end )====================

===================( engines/builder/src/gen_fingerprint.sh )====================
#!/bin/bash
[ -z "$PROJECT_NAME" ] && echo "no project name" && exit 1
TOML="$1"
TS=$(date '+%Y-%m-%d_%H-%M-%S')
HASH=$(pip freeze 2>/dev/null | sha256sum | cut -d' ' -f1)

echo "hash=$HASH"
echo "generated_at=$TS"
echo "generated_by=$USER"

export FINGERPRINT_HASH=$HASH
export FINGERPRINT_TS=$TS

[ -z "$TOML" ] && exit 0
[ ! -f "$TOML" ] && exit 0

sed -i "s|^hash.*=.*|hash            = \"$HASH\"|" "$TOML"
sed -i "s|^generated_at.*=.*|generated_at    = \"$TS\"|" "$TOML"
sed -i "s|^generated_by.*=.*|generated_by    = \"$USER\"|" "$TOML"
===================( end )====================

===================( engines/builder/src/install_deps.sh )====================
#!/bin/bash
TOML="$1"
[ -z "$TOML" ] && echo "no toml provided" && exit 1

DEPS=$(grep "^requires" "$TOML" | cut -d'=' -f2 | tr -d '[]"' | tr ',' ' ')

[ -z "$DEPS" ] && echo "no dependencies found" && exit 0

echo "installing: $DEPS"
pip install $DEPS --break-system-packages
===================( end )====================

===================( engines/builder/src/read_toml.sh )====================
#!/bin/bash
TOML="$1"
[ -z "$TOML" ] && echo "no toml provided" && exit 1
[ ! -f "$TOML" ] && echo "toml not found: $TOML" && exit 1

get_val() {
  grep "^$1" "$TOML" | cut -d'=' -f2 | tr -d ' "' 
}

PROJECT_NAME=$(get_val "name")
PROJECT_TYPE=$(get_val "domain")
PROJECT_ENV=$(get_val "env")
PROJECT_PYTHON=$(get_val "python")

export PROJECT_NAME PROJECT_TYPE PROJECT_ENV PROJECT_PYTHON
===================( end )====================

===================( engines/builder/src/register.sh )====================
#!/bin/bash
[ -z "$PROJECT_NAME" ] && echo "no project name" && exit 1

REG_LOG=~/project/project_path/data/registry.txt
REG_CONF=~/project/project_path/data/registry.conf
TOML="$1"
TS=$(date '+%Y-%m-%d_%H-%M-%S')
NODE=$(hostname)
UUID=$(cat /proc/sys/kernel/random/uuid)

echo "$TS :: $USER :: $PROJECT_NAME :: $PROJECT_TYPE :: $PROJECT_ENV :: $FINGERPRINT_HASH" >> "$REG_LOG"

if ! grep -q "^$PROJECT_NAME:" "$REG_CONF" 2>/dev/null; then
  echo "$PROJECT_NAME:~/project/$PROJECT_NAME:$PROJECT_VENV:$PROJECT_ENV:$PROJECT_TYPE" >> "$REG_CONF"
  bash ~/project/project_path/data/log/registry/src/on_register.sh "$PROJECT_NAME" "$TS"
else
  sed -i "s|^$PROJECT_NAME:.*|$PROJECT_NAME:~/project/$PROJECT_NAME:$PROJECT_VENV:$PROJECT_ENV:$PROJECT_TYPE|" "$REG_CONF"
  bash ~/project/project_path/data/log/registry/src/on_update.sh "$PROJECT_NAME" "$TS"
fi

[ -f "$TOML" ] && sed -i "s|^project_id.*=.*|project_id  = \"$UUID\"|" "$TOML"
[ -f "$TOML" ] && sed -i "s|^node.*=.*|node        = \"$NODE\"|" "$TOML"

echo "registered: $PROJECT_NAME :: $UUID :: $NODE"
===================( end )====================

===================( engines/builder/src/stamp_folders.sh )====================
#!/bin/bash
[ -z "$PROJECT_NAME" ] && echo "no project name" && exit 1
BASE="$HOME/project/$PROJECT_NAME"
mkdir -p "$BASE"/{src,input,output,tests,docs}
echo "stamped: $BASE"
===================( end )====================

===================( engines/drift_engine/src/main.sh )====================
===================( end )====================

===================( engines/log_engine/src/main.sh )====================
===================( end )====================

===================( engines/network/src/main.sh )====================
===================( end )====================

===================( engines/state_engine/src/main.sh )====================
===================( end )====================
